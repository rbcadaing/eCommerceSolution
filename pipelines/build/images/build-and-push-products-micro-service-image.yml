
resources:
- repo: self

variables:
  - group: docker-hub
  - name: tag
    value: '$(Build.BuildId)'
  - name: vmImageName
    value: 'ubuntu-latest'

stages:
- stage: Build
  displayName: Build image
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: ubuntu-latest
    steps:
   
    - script: |
        docker login -u rbcadaing -p $(docker_pw)
        docker compose -f docker-compose.build.yaml build products-microservice
        docker tag rbcadaing/products-microservice:latest rbcadaing/products-microserviceateway:$(tag)
        docker image ls
        docker push rbcadaing/products-microservice:$(tag)
      workingDirectory: '$(Build.SourcesDirectory)'
      displayName: "Build And Push Products Microservice"

