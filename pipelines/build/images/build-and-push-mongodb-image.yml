
resources:
- repo: self

variables:
  - group: docker-hub

stages:
- stage: Build
  displayName: Build image
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: ubuntu-latest
    steps:
   
    - script: |
        docker login -u rbcadaing -p $(docker_pw)
        docker build -t rbcadaing/ecommerce-mongodb:latest .
        docker tag rbcadaing/ecommerce-mongodb:latest rbcadaing/ecommerce-mongodb:$(tag)
        docker image ls
        docker push rbcadaing/ecommerce-mongodb:$(tag)
      workingDirectory: '$(Build.SourcesDirectory)/deployment/mongodb'
      displayName: "Run script in subfolder"

