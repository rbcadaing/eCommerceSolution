
resources:
- repo: self

variables:
  - group: docker-hub
  - name: tag
    value: '$(Build.BuildId)'
  - name: vmImageName
    value: 'ubuntu-latest'
    
stages:
- stage: Build
  displayName: Build image
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: ubuntu-latest
    steps:
   
    - script: |
        docker login -u rbcadaing -p $(docker_pw)
        docker build -t rbcadaing/ecommerce-postgres:latest .
        docker tag rbcadaing/ecommerce-postgres:latest rbcadaing/ecommerce-postgres:$(tag)
        docker image ls
        docker push rbcadaing/ecommerce-postgres:$(tag)
      workingDirectory: '$(Build.SourcesDirectory)/deployment/postgres'
      displayName: "Build and Push Image to Docker Hub"

